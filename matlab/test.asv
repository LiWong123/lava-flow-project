f = func(5,6)

function f = func(a, b, c)
    if nargin > 1
        f = subfunc(b, c);
    else 
        f = 1;
    end

end

function f = subfunc(b, c)
    if nargin > 1
        f = 3;
    else 
        f = 2;
    end
    
end