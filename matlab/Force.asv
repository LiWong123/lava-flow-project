classdef Force < handle

    properties (Access = public)
        
        pde;

    end

    methods

        function obj = Force(pde)
            % constructor
            obj.pde = pde;
        end

        function force = calculateForce(obj, edges)
           
            
        end

        function integrand = integrand(obj, x, y)

            

        end

    end



end